name: Keep HF Space Alive

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping HF Space
        run: curl -sf https://joshijii-sherpa-engine.hf.space || echo "Ping failed, space might be cold starting"